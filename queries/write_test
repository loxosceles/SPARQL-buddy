dbo rdf rdfs dbp
SELECT DISTINCT ?searchterm ?type
    WHERE {
       ?searchterm rdf:type ?o  ;
                  foaf:name ?name
      FILTER(STR(?name) = "Alan Turing") .
      ?searchterm rdf:type ?type
      FILTER(STR(?type) = "http://xmlns.com/foaf/0.1/Person") .

    } LIMIT 10
